.task
bin
# Keep dist tracked as an empty placeholder so go:embed compiles on a clean checkout.
!frontend/dist/
frontend/dist/*
!frontend/dist/.gitkeep
frontend/node_modules
build/linux/appimage/build
build/windows/nsis/MicrosoftEdgeWebview2Setup.exe

# Wails build config lives here. Re-include it in case a global ignore rule hides build/.
!build/
