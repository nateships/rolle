import { defineMeta } from "blume";

export default defineMeta({
  pages: ["index", "install", "quickstart", "desktop", "aws", "azure", "gcp", "cli", "mise", "security", "roadmap", "troubleshooting"],
});
