<footer>
  <ul class="facts">
    <li class="lock">
      <svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="11" width="18" height="10" rx="2" /><path class="shackle" d="M7 11V7a5 5 0 0 1 10 0v4" /></svg>
      <div><b>Nothing long-lived on disk</b><span>Refresh tokens stay in the OS keychain. Short-lived files die with the session.</span></div>
    </li>
    <li class="shell">
      <svg viewBox="0 0 24 24" aria-hidden="true"><path class="prompt" d="M4 17l6-5-6-5" /><path class="cursor" d="M12 19h8" /></svg>
      <div><b>Your tools, unchanged</b><span>AWS profiles through credential_process. Azure and Google Cloud tokens in your shell.</span></div>
    </li>
    <li class="eye">
      <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M10.6 10.6a2 2 0 0 0 2.8 2.8M9.9 5.1A10 10 0 0 1 12 5c6 0 9.5 7 9.5 7a17 17 0 0 1-3.2 3.9M6.6 6.6C3.8 8.5 2.5 12 2.5 12s3.5 7 9.5 7a9.7 9.7 0 0 0 4.2-.9" /><path class="slash" d="M3 3l18 18" /></svg>
      <div><b>No telemetry</b><span>Free and open source. macOS, Windows, and Linux, with signed self-updates.</span></div>
    </li>
  </ul>
</footer>

<style>
  /* Three facts, each with its mark. */
  footer {
    border-top: 1px solid var(--border);
    padding-block: 1.25rem 1.4rem;
    color: var(--muted);
  }

  .facts {
    display: grid;
    gap: 1rem 2.5rem;
    padding: 0;
    list-style: none;
  }

  @media (min-width: 58rem) {
    .facts {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .facts li {
    display: grid;
    grid-template-columns: 1.75rem 1fr;
    gap: 0.9rem;
    align-items: start;
  }

  .facts svg {
    width: 1.75rem;
    height: 1.75rem;
    fill: none;
    stroke: var(--text);
    stroke-width: 1.6;
    stroke-linecap: round;
    stroke-linejoin: round;
    margin-top: 0.1rem;
    overflow: visible;
  }

  /* The marks draw themselves in on load, one after another. */
  .facts svg path,
  .facts svg rect {
    stroke-dasharray: 80;
    stroke-dashoffset: 80;
    animation: draw 0.9s var(--ease) both;
    animation-delay: calc(0.5s + var(--i) * 0.14s);
  }

  .facts li:nth-child(1) { --i: 0; }
  .facts li:nth-child(2) { --i: 1; }
  .facts li:nth-child(3) { --i: 2; }

  @keyframes draw {
    to {
      stroke-dashoffset: 0;
    }
  }

  /* On hover the mark grows and glows, the text brightens, and each mark
     does the one thing it means. */
  .facts li {
    cursor: default;
  }

  .facts svg {
    transform-origin: 50% 60%;
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), filter 0.3s var(--ease);
  }

  .facts li:hover svg {
    transform: scale(1.3) translateY(-2px);
    filter: drop-shadow(0 0 10px light-dark(rgb(16 17 20 / 0.25), rgb(244 240 232 / 0.45)));
  }

  .facts span {
    transition: color 0.3s;
  }

  .facts li:hover span {
    color: var(--text);
  }

  .lock .shackle {
    transform-origin: 7px 11px;
    transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  .lock:hover .shackle {
    transform: translateY(-3.5px) rotate(-18deg);
  }

  .shell .prompt {
    transition: transform 0.25s var(--ease);
  }

  .shell:hover .prompt {
    transform: translateX(3px);
  }

  .shell:hover .cursor {
    animation: blink 0.7s steps(2) infinite;
  }

  @keyframes blink {
    to {
      opacity: 0;
    }
  }

  .eye path:not(.slash) {
    transition: opacity 0.3s;
  }

  .eye:hover path:not(.slash) {
    opacity: 0.35;
  }

  .eye:hover .slash {
    animation: draw 0.5s var(--ease) both;
    stroke-width: 2.2;
  }

  @media (prefers-reduced-motion: reduce) {
    .facts svg path,
    .facts svg rect {
      animation: none;
      stroke-dashoffset: 0;
    }

    .lock .shackle,
    .shell .prompt {
      transition: none;
    }

    .shell:hover .cursor,
    .eye:hover .slash {
      animation: none;
    }
  }

  .facts b {
    display: block;
    font-family: var(--display);
    font-weight: 600;
    font-size: clamp(1.05rem, 1.25vw, 1.25rem);
    letter-spacing: -0.015em;
    line-height: 1.2;
    color: var(--text);
    margin-bottom: 0.2rem;
  }

  .facts span {
    font-size: 0.9rem;
    line-height: 1.45;
  }
</style>
